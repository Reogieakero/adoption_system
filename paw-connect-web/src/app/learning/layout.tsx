import ResidentLayout from '@/components/resident/resident-layout';

export default function LearningLayout({ children }: { children: React.ReactNode }) {
  return <ResidentLayout>{children}</ResidentLayout>;
}
